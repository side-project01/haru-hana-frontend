import Onboarding from './features/onboarding/Onboarding'
import './App.css'

function App() {
  return (
    <div className="app">
      {/* TODO: 위저드 스텝 라우팅 (src/app, src/features)
          온보딩 완료(onComplete) → 오늘의 질문 화면 연결 예정 */}
      <Onboarding />
    </div>
  )
}

export default App
